# 📱 คู่มือการตั้งค่า LINE Official Account (LINE OA)

## 🎯 ภาพรวม

ระบบ Student Care สามารถส่งการแจ้งเตือนไปยังผู้ปกครองผ่าน LINE OA ได้ เช่น:
- 🟢 แจ้งเตือนเมื่อบุตรเข้าโรงเรียน
- 🟠 แจ้งเตือนเมื่อบุตรออกจากโรงเรียน
- ⚠️ แจ้งเตือนเมื่อบุตรขาดเรียน
- 📝 แจ้งเตือนพฤติกรรมผิดปกติ

---

## 📋 ขั้นตอนการตั้งค่า

### 1. สร้าง LINE Official Account

1. ไปที่ https://manager.line.biz/
2. คลิก "สร้างบัญชี Official Account"
3. กรอกข้อมูล:
   - ชื่อบัญชี: "โรงเรียน XXX - Student Care"
   - ประเภทธุรกิจ: "การศึกษา"
   - อุตสาหกรรม: "โรงเรียน"
4. คลิก "สร้าง"

### 2. ตั้งค่า Messaging API

1. ใน LINE Official Account Manager
2. ไปที่ "Settings" > "Messaging API"
3. คลิก "Enable Messaging API"
4. เลือก Provider (หรือสร้างใหม่)
5. คลิก "OK"

### 3. ดึง Channel Access Token

1. ใน Messaging API Settings
2. หา "Channel access token"
3. คลิก "Issue" เพื่อสร้าง Token
4. คัดลอก Token (เก็บไว้ใช้ในขั้นตอนถัดไป)

**ตัวอย่าง Token:**
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### 4. ตั้งค่า Webhook URL

1. ใน Messaging API Settings
2. หา "Webhook settings"
3. คลิก "Edit"
4. ใส่ URL:
   ```
   https://your-domain.com/webhook/line
   ```
   หรือถ้าใช้ ngrok (สำหรับทดสอบ):
   ```
   https://xxxx-xx-xxx-xxx-xxx.ngrok.io/webhook/line
   ```
5. เปิด "Use webhook" = ON
6. คลิก "Verify" เพื่อทดสอบ
7. คลิก "Update"

### 5. ตั้งค่าใน Student Care System

#### วิธีที่ 1: ใช้ไฟล์ .env (แนะนำ)

สร้างหรือแก้ไขไฟล์ `.env`:

```bash
# LINE OA Configuration
LINE_CHANNEL_ACCESS_TOKEN=your_channel_access_token_here
LINE_CHANNEL_SECRET=your_channel_secret_here
```

#### วิธีที่ 2: แก้ไขไฟล์ line_oa.py

แก้ไขไฟล์ `line_oa.py`:

```python
class LineOA:
    def __init__(self):
        self.channel_access_token = 'YOUR_CHANNEL_ACCESS_TOKEN'
        self.channel_secret = 'YOUR_CHANNEL_SECRET'
```

### 6. ทดสอบการทำงาน

1. เปิดแอป LINE บนมือถือ
2. Add Friend โดยสแกน QR Code จาก LINE Official Account Manager
3. ส่งข้อความ "ทดสอบ" ไปยัง LINE OA
4. ระบบควรตอบกลับ

---

## 🔗 การเชื่อมต่อผู้ปกครอง

### วิธีที่ 1: ผู้ปกครองส่งรหัสนักเรียน

1. ผู้ปกครอง Add Friend LINE OA
2. ส่งรหัสนักเรียน เช่น "STD001"
3. ระบบจะเชื่อมต่ออัตโนมัติและตอบกลับ:
   ```
   ✅ เชื่อมต่อสำเร็จ!
   
   👤 ชื่อ: นายสมชาย ใจดี
   🏫 ห้อง: ม.1/1
   
   ท่านจะได้รับการแจ้งเตือนเมื่อ:
   🟢 บุตรเข้าโรงเรียน
   🟠 บุตรออกจากโรงเรียน
   ⚠️ บุตรขาดเรียน
   📝 พฤติกรรมผิดปกติ
   ```

### วิธีที่ 2: Admin เพิ่ม LINE User ID

1. ไปที่ `/student/<student_id>`
2. คลิก "เชื่อมต่อ LINE"
3. ใส่ LINE User ID
4. คลิก "บันทึก"

---

## 📝 ตัวอย่างการใช้งาน

### ส่งข้อความแจ้งเตือน

```python
from line_oa import line_oa

# ส่งข้อความธรรมดา
line_oa.send_message(line_user_id, "สวัสดีครับ")

# ส่งการแจ้งเตือนเข้าโรงเรียน
line_oa.send_gate_entry(
    line_user_id, 
    student_name="นายสมชาย ใจดี",
    entry_type="checkin",
    time="07:30 น."
)

# ส่งการแจ้งเตือนออกจากโรงเรียน
line_oa.send_gate_entry(
    line_user_id,
    student_name="นายสมชาย ใจดี", 
    entry_type="checkout",
    time="16:00 น."
)
```

### ตรวจสอบ LINE User ID

```python
# ดึง LINE User ID ของนักเรียน
line_user_id = db.get_student_line_token(student_id)

if line_user_id:
    print(f"นักเรียนมี LINE User ID: {line_user_id}")
else:
    print("นักเรียนยังไม่ได้เชื่อมต่อ LINE")
```

---

## 🔧 การแก้ไขปัญหา

### ปัญหา: Webhook ไม่ทำงาน

**สาเหตุ:**
- URL ไม่ถูกต้อง
- Server ไม่ได้เปิด HTTPS
- Firewall บล็อก

**วิธีแก้:**
1. ตรวจสอบ URL ว่าเป็น HTTPS
2. ใช้ ngrok สำหรับทดสอบ:
   ```bash
   ngrok http 5000
   ```
3. คัดลอก HTTPS URL จาก ngrok ไปใส่ใน Webhook URL

### ปัญหา: ส่งข้อความไม่ได้

**สาเหตุ:**
- Channel Access Token ไม่ถูกต้อง
- LINE User ID ไม่ถูกต้อง
- ผู้ใช้บล็อก LINE OA

**วิธีแก้:**
1. ตรวจสอบ Token ใน `.env`
2. ตรวจสอบ LINE User ID ในฐานข้อมูล
3. ให้ผู้ใช้ Unblock LINE OA

### ปัญหา: ไม่ได้รับข้อความจากผู้ใช้

**สาเหตุ:**
- Webhook URL ไม่ถูกต้อง
- "Use webhook" ไม่ได้เปิด

**วิธีแก้:**
1. ตรวจสอบ Webhook URL
2. เปิด "Use webhook" = ON
3. คลิก "Verify" เพื่อทดสอบ

---

## 📊 ตัวอย่าง Flow การทำงาน

```
1. นักเรียนเข้าโรงเรียน
   ↓
2. กล้องประตูตรวจจับใบหน้า
   ↓
3. ระบบบันทึกการเข้าเรียน
   ↓
4. ดึง LINE User ID ของผู้ปกครอง
   ↓
5. ส่งข้อความแจ้งเตือนผ่าน LINE OA
   ↓
6. ผู้ปกครองได้รับการแจ้งเตือนบนมือถือ
```

---

## 🔐 ความปลอดภัย

### ⚠️ สิ่งที่ต้องระวัง:

1. **อย่า commit Token ลง Git**
   - ใช้ `.env` และเพิ่ม `.env` ใน `.gitignore`

2. **เก็บ Token ให้ปลอดภัย**
   - ใช้ Environment Variables
   - ใช้ Secret Manager (AWS Secrets Manager, etc.)

3. **ตรวจสอบ Webhook Signature**
   - ระบบมีการตรวจสอบ signature อยู่แล้วใน `line_oa.py`

---

## 📚 เอกสารเพิ่มเติม

- [LINE Messaging API Documentation](https://developers.line.biz/en/docs/messaging-api/)
- [LINE Official Account Manager](https://manager.line.biz/)
- [LINE Developers Console](https://developers.line.biz/console/)

---

## 💡 Tips

1. **ใช้ Rich Menu** - สร้างเมนูด้านล่างให้ผู้ปกครองกดได้ง่าย
2. **ใช้ Flex Message** - ส่งข้อความแบบสวยงามพร้อมรูปภาพ
3. **ใช้ Quick Reply** - ให้ผู้ปกครองตอบกลับได้ง่าย
4. **ตั้งค่า Auto Reply** - ตอบกลับอัตโนมัติเมื่อผู้ใช้ Add Friend

---

## 🆘 ติดต่อสอบถาม

หากมีปัญหาหรือข้อสงสัย:
- Email: support@softubon.com
- GitHub Issues: https://github.com/Yanperm/hikvission_student_care/issues

---

© 2025 SOFTUBON CO.,LTD. - Student Care System
