# 🚀 วิธีเปิดใช้งาน Hikvision Face Recognition

## ✅ ขั้นตอนที่ 1: ตั้งค่ากล้อง Hikvision

### 1.1 เข้าเว็บกล้อง
```
http://192.168.1.64
Username: admin
Password: [รหัสผ่านของคุณ]
```

### 1.2 เปิดใช้งาน Face Recognition
```
Configuration → Face Recognition → Enable
Configuration → Event → Face Detection → Enable
Configuration → Network → Advanced → Integration Protocol → Enable
```

### 1.3 ตั้งค่า Event Notification
```
Configuration → Event → Smart Event → Face Detection
- Enable Face Detection
- Arming Schedule: 24/7
- Linkage Method: Notify Surveillance Center
```

---

## ✅ ขั้นตอนที่ 2: ตั้งค่าระบบ

### 2.1 แก้ไขไฟล์ `.env`
```bash
# เปิดใช้งาน Hikvision
USE_HIKVISION=true

# ตั้งค่ากล้อง
CAMERA_IP=192.168.1.64
CAMERA_USER=admin
CAMERA_PASS=your_password
```

### 2.2 รีสตาร์ทระบบ
```bash
# หยุดระบบเดิม
Ctrl+C

# รันใหม่
python local_app.py
```

---

## ✅ ขั้นตอนที่ 3: Sync นักเรียนเข้ากล้อง

### วิธีที่ 1: Sync ทีละคน (อัตโนมัติ)
```
เมื่อเพิ่มนักเรียนใหม่ → ระบบจะ Sync ไปกล้องอัตโนมัติ
```

### วิธีที่ 2: Sync ทั้งหมด (Manual)
```python
# สร้างไฟล์ sync_to_camera.py
from hikvision_face_api import init_hikvision
from database import db

camera = init_hikvision('192.168.1.64', 'admin', 'password')
students = db.get_students('SCH001')

# กรองเฉพาะที่มีรูป
students_with_images = [s for s in students if s.get('image_path')]

# Sync
result = camera.sync_all_students(students_with_images)
print(f"Sync สำเร็จ: {result['success']}/{result['total']} คน")
```

รัน:
```bash
python sync_to_camera.py
```

---

## ✅ ขั้นตอนที่ 4: ทดสอบระบบ

### 4.1 ตรวจสอบการเชื่อมต่อ
เมื่อรันระบบจะเห็น:
```
✅ Hikvision Camera Connected
🎬 Hikvision Event Listener Started
```

### 4.2 ทดสอบจับใบหน้า
1. ยืนหน้ากล้อง (ระยะ 1-3 เมตร)
2. ระบบจะแสดง:
```
✅ บันทึกการเข้าเรียน: สมชาย ใจดี (98.5%)
```

### 4.3 ตรวจสอบ Log
```bash
tail -f app.log
```

---

## 🔧 Troubleshooting

### ❌ ไม่สามารถเชื่อมต่อกล้อง
```
ตรวจสอบ:
1. IP Address ถูกต้องหรือไม่
2. Username/Password ถูกต้องหรือไม่
3. กล้องเปิดอยู่หรือไม่
4. Ping ได้หรือไม่: ping 192.168.1.64
```

### ❌ ไม่ได้รับ Event
```
ตรวจสอบ:
1. เปิด Event Notification ในกล้องหรือยัง
2. Firewall บล็อคหรือไม่
3. Firmware เวอร์ชันล่าสุดหรือไม่
```

### ❌ จับใบหน้าไม่ได้
```
ตรวจสอบ:
1. Sync นักเรียนเข้ากล้องหรือยัง
2. รูปภาพชัดเจนหรือไม่
3. แสงเพียงพอหรือไม่
4. ระยะห่าง 1-3 เมตร
```

---

## 📊 เปรียบเทียบ

| ฟีเจอร์ | ก่อน (OpenCV) | หลัง (Hikvision) |
|---------|---------------|------------------|
| ความแม่นยำ | 60-70% | 99%+ |
| ความเร็ว | ช้า | เร็วมาก |
| Real-time | ❌ | ✅ |
| CPU Usage | สูง | ต่ำ |
| ระยะจับ | 1-2m | 3-5m |
| แจ้งเตือน LINE | ❌ | ✅ |

---

## 🎯 สรุป

เมื่อเปิดใช้งาน Hikvision:
1. ✅ ความแม่นยำสูงขึ้น 99%+
2. ✅ ไม่ต้องประมวลผลเอง (กล้องทำให้)
3. ✅ Real-time Event
4. ✅ แจ้งเตือน LINE อัตโนมัติ
5. ✅ บันทึกการเข้าเรียนอัตโนมัติ

---

© 2025 SOFTUBON CO.,LTD.
